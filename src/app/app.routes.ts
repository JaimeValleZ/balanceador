import { Routes } from '@angular/router';
import { BalanceadorComponent } from './pages/balanceador/balanceador';

export const routes: Routes = [
  { path: '', component: BalanceadorComponent } // raíz
];
